# Monica Steam 第四个公开测试版本

这是第四个公开测试版本的最新变更说明。本版本仍处于测试阶段，并非正式版或稳定版。

> [!WARNING]
> 升级前建议备份现有 `maFile`、MDBX 数据及其他重要账号资料。部分功能依赖 Steam
> 服务、账号权限和所在地区，实际可用性可能受 Valve 的接口与风控策略影响。

## Steam 链接

- Android 现在可以将 `s.team`、`steamcommunity.com` 和
  `store.steampowered.com` 链接交给 Monica Steam 打开。
- Steam 商店商品链接会直接进入 Monica Steam 的原生游戏详情页。
- 包含数字 SteamID 的社区个人资料链接会直接进入原生社区资料页。
- Steam 短链接、昵称资料页及其他可信 Steam 页面会使用现有的应用内浏览器打开。
- 冷启动和应用已在后台运行两种情况均可正确接收链接，并拒绝仿冒 Steam 域名或非网页协议。

## 商店与喜加一

- 喜加一和永久免费游戏新增直接领取入库操作，并修复免费许可证提交不稳定的问题。
- 直接领取不可用时会进入带当前 Steam 登录会话的官方页面继续处理，避免网页重新登录。
- 商店新增“激活产品代码”快捷入口，可直接打开 Steam 官方 CD Key 激活页面。
- 商店首页及筛选目录会按当前 Steam 账号所在地区显示实际币种，例如印度区显示印度卢比。
- 商店忽略功能改为按 SteamID 保存的本地优先模式；点击“忽略 / 取消忽略”后会立即更新详情、首页、筛选目录、搜索结果和本地缓存，无需等待 Steam 响应，离线状态下同样有效。
- Steam 官方忽略列表会导入 Monica Steam，本地待同步操作会优先于旧的官方状态；Steam 同步失败时保持本地结果并在后续商店请求中重试，界面不会回滚。
- “商店与游戏提示”设置新增“同步 Steam 忽略列表”开关。关闭后屏蔽仅在 Monica Steam 中生效，开启后会在后台同步至 Steam 官方账号。
- 修复 Steam 商店偏好会话失效时主页、目录或详情被整体阻断的问题；偏好同步现在会保留缓存并降级处理，不再影响主要商店内容加载。
- 修复商店主页加载失败后因账号状态重复更新而持续清空并重新加载的问题。
- 游戏库“游戏数分布”的价格统计独立统一换算为人民币，再进入人民币价格区间；未知汇率不会被错误计入。

## 游戏库与个人游戏内容

- 游戏详情中的“游戏数据”和“游戏截图”入口改为并排紧凑按钮，仅在对应功能可用时显示。
- CS2 与 Dota 2 的游戏库详情页新增“个人游戏数据”入口，可直接查看当前账号的比赛、活动及统计记录，无需在 Steam 官方客户端中逐层查找。
- 个人游戏数据页面复用当前 Steam 社区登录会话；下载比赛回放时会将链接交给外部浏览器处理，不在应用内重复实现下载器。
- 游戏库详情页的“游戏截图”已改为 Monica Steam 原生页面，按当前 Steam 账号和游戏 AppID 显示已上传至 Steam 社区的截图；电脑本地尚未上传的截图不会显示。
- 原生截图页支持双列网格、下拉刷新、自动分页、空状态与错误重试；点击截图可全屏左右滑动、缩放并直接下载原图。
- 从个人游戏数据或截图页面返回时，会回到原游戏详情页并保留当前浏览路径。

## 导航与显示

- 设置页顶部新增与 Monica Pass 安全分析一致的网络优化下拉卡片；卡片会随下拉距离从紧凑形态过渡为大卡片，达到阈值后提供震动反馈，松手或点按会进入独立网络优化页面。
- 独立网络优化页面可同时查询系统 DNS、DNSPod、AliDNS、Cloudflare、Google 与 Quad9 ECS，并保留高级网络设置子页面管理手动 Hosts。
- 网络优化会对 Steam 商店、社区和 API 域名执行 HTTPS 证书验证与延迟测试，三个必需域名全部可用后才保存最快节点；扫描失败时保留原有设置。
- 自动优选结果写入 Monica Steam 的应用内 Hosts，并保留高级页面中的手动规则。该功能仅作用于受支持的原生网络请求，不修改 Android 系统 DNS、系统 Hosts、VPN、其他应用或 WebView。
- 设置页参考 Monica Pass 的分区层级重新整理为“安全与数据、界面与交互、Steam 体验、应用”四组；Dock 与减少动效、商店提示与链接、通知与网络、语言与扩展不再混放在同一页面。
- 设置首页搜索会继续匹配各子页面中的具体选项，进入子分类再返回时会分别保留首页和各分类的滚动位置。
- Steam 内置浏览器已从商店 UI 中彻底拆分为独立 `steam/web` 模块，商店、礼物、个人游戏数据和游戏截图统一使用同一套浏览器内核。
- Steam 网页改为全屏显示，浏览时隐藏原页面顶栏和 Dock，并复用令牌多选胶囊操作栏提供网页后退、前进、刷新、分享、外部浏览器打开及返回 Monica Steam。
- 修复进入 Steam 网页时页面提交回调无限递归并触发 `StackOverflowError` 的闪退，同时修正页面完成、网页权限与全屏媒体回调中的同类隐患。
- 重做 WebView 首帧与生命周期：在页面内容提交前保持匹配页面的稳定底色，保存和恢复浏览历史，前后台切换时正确暂停与恢复页面，减少进入、退出和液态玻璃模式切换时的白屏或闪烁。
- 新增主页面网络、HTTP、SSL 与不安全跳转错误页；WebView 渲染进程异常退出时会销毁失效实例并限次自动重建，避免直接带崩应用。
- 内置浏览器现在支持 Steam 官方网页文件选择、仅限 Steam 官方来源的摄像头/麦克风权限请求以及网页全屏视频；未知网页权限、文件协议、混合内容、第三方 Cookie 和非 Steam 站内跳转继续默认拒绝。
- 网页下载继续交给系统外部浏览器处理，个人游戏数据中的比赛回放无需在 Monica Steam 内重复维护下载器。
- 新增 Monica Pass 风格的固定底栏，与 M3E、液态玻璃共同组成三种可选底栏样式。
- 修正固定底栏、液态玻璃底栏和系统导航栏下方的安全间距，避免设置项、按钮及列表末尾被遮挡。
- 关闭 Android 预测式返回动画，底部弹窗和选择器返回时不再出现侧边预览形变，行为与 Monica Pass 保持一致。
- 设置子页面返回后的内容位置和底部缓冲区得到进一步修正。
- 修复部分 Steam 动态个人资料背景停止播放的问题。

## 免责声明

Monica Steam 是非官方第三方客户端，与 Valve Corporation 或 Steam 没有隶属、授权或
合作关系。Steam、Steam Guard 及相关商标归 Valve Corporation 所有。
