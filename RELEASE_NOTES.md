# Monica Steam 第四个公开测试版本

第四个公开测试版本正在准备中。本文件已清空重建，后续完成的功能和修复将继续追加到这里。

> [!WARNING]
> 这是公开测试版本，并非正式版或稳定版。升级前建议备份现有 `maFile`、MDBX 数据和
> 其他重要账号资料。

## 当前变更

### 实时聊天通知

- 收到 Steam 私聊消息时，使用独立高优先级 `MessagingStyle` 通知实时显示发送者和消息内容。
- 点击消息通知会切换到对应 Steam 账号，并直接进入准确的好友会话。
- 后台连接不再长期显示“Steam 聊天正在运行”或“正在监听某账号”的状态文案。
- Android 要求持续 Steam CM 连接必须保留前台服务通知，因此现在只保留静默、低干扰的
  系统服务占位，不播放声音、不震动、不显示时间，也不会在锁屏上展示内容。
- 服务占位与真正的聊天消息使用不同通知频道；收到的消息仍会正常提醒并显示内容。
- 增加通知权限和消息通知频道状态检查，关闭消息通知时不会错误标记为已经送达。
- 通知发布失败后会释放去重记录，后续收到重放事件时可以重新通知，避免消息静默丢失。
- 增加前台服务展示、消息通知、去重恢复和通知跳转的回归测试。

### Steam 语音通话

- 修复聊天实时连接与语音信令同时启动时，同一账号的 Steam CM 连接互相关闭并显示
  `Steam CM is unavailable` 的问题。
- 同一账号现在会共享正在登录的 CM WebSocket 和单次获取的临时聊天令牌，降低语音接通时的
  随机失败概率。
- 语音状态只有在麦克风音轨可用、ICE 已连接、Steam 会话数据已更新且直连通话已被接听后，
  才会显示为已连接。
- 增加临时网络故障的有限重试、WebRTC 兼容回退、远端音频播放诊断、麦克风上行监测和媒体
  恢复机制。
- 来电通知的接听、拒绝、静音和挂断操作改由通话前台服务处理；等待接听期间不会提前占用
  麦克风，通话开始后才启用音频焦点、通话设备路由和息屏保持。
- 修正 Android 14 前台服务类型切换，减少后台、锁屏和通知操作导致的语音中断。
- 增加 CM 并发、信令重试、媒体就绪、前台服务资源状态及 WebRTC 恢复的回归测试；真实 Steam
  会话测试已确认 ICE 连接和麦克风音频数据上行。

## 免责声明

Monica Steam 是非官方第三方客户端，与 Valve Corporation 或 Steam 没有隶属、授权或
合作关系。Steam、Steam Guard 及相关商标归 Valve Corporation 所有。
