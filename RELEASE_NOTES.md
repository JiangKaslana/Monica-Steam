# Monica Steam 1.0.304 第三个公开测试版本补丁

本补丁基于 Monica Steam `1.0.304` 第三个公开测试版本，集中处理发布后发现的
稳定性、设置结构和界面交互问题。

> [!WARNING]
> 这仍是公开测试版本的补丁，并非正式版或稳定版。升级前建议备份现有 `maFile`、
> MDBX 数据和其他重要账号资料。

## 稳定性修复

- 修复仅登录账号缺少 `sharedSecret` 时仍尝试生成 Steam Guard 验证码，进而触发
  `IllegalArgumentException: Empty key` 主线程崩溃的问题。
- 空白、无效 Base64 或解码后为空的验证码密钥现在会被安全忽略，不再传入加密组件。
- 令牌刷新只会为具备有效验证器密钥的账号生成验证码。
- 增加仅登录账号、空密钥和设置入口的回归守护测试。

## 设置与个性化

- 设置首页改为紧凑分类入口，仅显示“数据与存储”“Steam 功能”和“外观与界面”。
- 具体设置移动到对应子页面，返回后会保留各页面原有的滚动位置。
- 普通头像与支持 Steam 头像框的头像可以分别设置形状，旧版单一头像设置会自动兼容迁移。
- “外观与界面”新增验证器进度条样式，可在波浪形和线形之间切换。
- 进度条样式选择器直接复用 Monica Android 的组件和持久化逻辑，修改后立即应用于
  令牌页顶部进度条。
- 选择器改为整行可点击，不再要求精确点击单选按钮。

## 界面与交互修复

- 修复长按游戏数量分布图时整列出现白色边框的问题，现在只突出显示实际柱体。
- 修复商店详情页购买 Split button 两侧高度和点击区域不一致的问题。
- 移除未投入使用的令牌账号组织、分组和标签功能，减少无效入口及多余状态逻辑。

## 升级说明

- 应用版本仍为 `1.0.304`，本补丁不修改版本号或数据格式。
- 现有账号、头像设置和令牌数据无需重新导入。
- 本说明覆盖提交 `8ce52f8` 之后至 `355d206` 的用户可见修改。
- 可查看本补丁的[完整提交对比][full-comparison]。

## 免责声明

Monica Steam 是非官方第三方客户端，与 Valve Corporation 或 Steam 没有隶属、授权或
合作关系。Steam、Steam Guard 及相关商标归 Valve Corporation 所有。

[full-comparison]: https://github.com/JoyinJoester/Monica-Steam/compare/8ce52f8...355d206
